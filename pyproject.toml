[project]
name = "takuto-portfolio"
version = "1.0"
description = "The portfolio website of Takuto Hamamichi."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "zensical>=0.0.41",
]
