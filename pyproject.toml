[project]
name = "Takuto's Portfolio"
version = "1.0"
description = "The portfolio website of Takuto Hamamichi."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
    "zensical>=0.0.41",
]
